---
"repp": patch
---

Move config to `plans/settings.json`

Settings now live alongside plans in `plans/settings.json` instead of `.repprc` at repo root. Added graceful handling for invalid plan frontmatter.
