---
"repp": minor
---

Replace `--min-priority` with `--priority` for exact matching. Accept comma-separated values (e.g., `--priority=1,2,3`) and support any alphanumeric priority format (1, 01, P1, high, etc.).
